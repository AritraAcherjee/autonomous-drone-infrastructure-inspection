# Existing Python 3.12.14 venv. Install detector runtime only, preserve unrelated packages.
# NumPy 2.5.3 -> 2.4.6 is required by the frozen detector specification.
-c generalization-inference-constraints.txt
-r detection.txt
