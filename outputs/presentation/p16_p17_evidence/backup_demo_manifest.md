# Offline backup-demo manifest

Status: **READY — verified presentation-source package**. The source ZIP remains authoritative; copied assets are hash-bound in `hashes.sha256`.

1. `dashboard/dashboard_overview.png` — original P16 dashboard overview.
2. `database/persisted_record.json` and `database/replay.json` — persisted record and duplicate/replay behavior.
3. `lineage.md` and `provenance/lineage.json` — P15 → P16 → P17 trace.
4. `report/inspection_2026091901.md` — accepted deterministic P17 report.
5. `presentation_notes.md` — truthful presenter wording and constraints.

The backup demo is presentation material, not a replacement scientific source. Retain the accepted confidence, `UNREVIEWED` state, and source/hash references.
